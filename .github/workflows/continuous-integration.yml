name: 継続的インテグレーション

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      uses: ./.github/workflows/dotnet-sdk-style-framework-desktop.yml
      with:
        module: 'FrameworkSdkStyleWpfApp'
